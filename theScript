#! /bin/bash

# Write the date + time of the commit to the log file
date >> log.txt

# Commit and push to remote
	# Bash does not like quotes in commands, so we save command as a variable and evaluate instead of just executing
COMMAND1='git commit -a -m "Todays commit"'
COMMAND2='git push'
eval $COMMAND1
eval $COMMAND2

